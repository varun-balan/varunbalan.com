// Counties visited, identified by 5-digit FIPS code (state + county).
// Add codes as a string array; e.g. Cook County, IL = "17031".
// I'll populate this from your list when you paste it.

const travel = {
  visited: [],
};

export default travel;
